{
  "name": "{{NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx zquery dev",
    "dev": "npx zquery dev",
    "build": "npx zquery build",
    "bundle": "npx zquery bundle"
  },
  "dependencies": {
    "zero-query": "latest"
  }
}
